public class MergeSort {

    // implementing mergesort in java
    // sort right half, sort left half, then sort

    public int[] msort(int[] array, int l, int m, int r){
        // sort array[] using mergesort
        // what we want to do is take the left and right sides, and merge recursively
        if(array.length==1){
            // this is our base case
            return array;
        }
        else{

        }
    }
}
